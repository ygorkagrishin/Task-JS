// task 1

/* var string = 'Привет! Как дела?';
var vowels = [ 'и','е','а','е','а', ];
var result = '';

for ( var i=0; i<=string.length - 1; i++ ) {
	var symbol = string.charAt( i );
	if ( vowels.indexOf( symbol ) !== -1 ) 
                result += symbol;
} */

// task 2 

/* var users = [
{
		name: 'Алексей',
		salary: 500
},
{
		name: 'Виктор',
		salary: 1300
},
{
		name: 'Иван',
		salary: 1500
}
];

function WhoIsThePoor(user) {

var rich = [];    

for ( var i=0; i<=user.length - 1; i++ ){
var subject = user[i]	
	if ( subject.salary >= 1000 ) 
            rich.push( subject.name ); }

return rich;

}; */

// task3 

/* var url = '/users/download/index.html';

function searchString( string ) {

if ( string.search( 'html' ) !== -1 )
	return true;
else return false;

}; */

// task 4

/* var arrayNumber = [3, 13, 74, 14, 66, 15, 22, 4];
var array = [];

function even( num ) {

for ( var i = 0; i<=num.length - 1; i++ ) {
var round = num[i];

	if ( round % 2  === 0 ) 
			array.push( round ); }

	return array;

}; */

//